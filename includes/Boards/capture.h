#ifndef SOURCE_REAR_CAMERA_H_
#define SOURCE_REAR_CAMERA_H_

////////////////////////////////////////////////////////////////////

#include "types.h"

////////////////////////////////////////////////////////////////////

typedef void ( *frame_hdl_t )( void * p_data, const void *p, uint16_t width, uint16_t height );

////////////////////////////////////////////////////////////////////

void * capture_start( const char * dev, frame_hdl_t hdl, void * p_data );
void capture_stop( void * n_cap );

////////////////////////////////////////////////////////////////////

#endif /* SOURCE_REAR_CAMERA_H_ */
